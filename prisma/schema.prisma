generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Task {
  id          String   @id @default(uuid())
  title       String
  dateTime    DateTime
  description String
  completed   Boolean  @default(false)

  @@map("tarefa")
}
